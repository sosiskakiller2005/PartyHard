<Window x:Class="PartyHard.Views.Windows.AddMasterClassWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PartyHard.Views.Windows"
        mc:Ignorable="d"
        Title="Добавить мастеркласс" Height="300" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height=".2*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

         <TextBlock Text="Добавление мастеркласса" FontSize="25" FontWeight="Bold" Margin="10"/>
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <TextBlock Text="Имя мастеркласса:"/>
            <TextBox Name="MasterClassNameTb" Margin="10" Width="300"/>
            <Button Name="AddMasterClassBtn" Content="Добавить" Margin="10" Click="AddMasterClassBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>
